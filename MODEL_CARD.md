# Model Card

## 模型身份与用途

名称：Chinese Guba AI Detector，历史权重版本标识：v1。维护者：GitHub用户`tjuwqx`。本次发布不提供公开权重下载。

基础模型是Chinese-BERT-wwm系列的 `hfl/chinese-roberta-wwm-ext`，实际加载结构为 `BertForSequenceClassification`，12层、隐藏维度768，二分类。基础模型本地缓存版本为 `5c58d0b8ec1d9014354d691c538661bf00bfdb44`。

本模型以通用中文AI文本检测为训练任务，用于股吧文本研究中的自动测量；没有金融专门预训练、股吧人工标注微调、模型水印识别、作者溯源或逐句AI定位能力。

## 历史训练记录

C-ReD来源：<https://github.com/HeraldofLight/C-ReD>，本地语料仓库版本 `24e03bb0b42ebfcdcebb11516054a1734b391136`。读取文件名含`CReD_`的CSV、去除空文本后为109,204条；不是论文完整语料的全量规模。原始标签0=AI、1=人工，转换为1=AI、0=人工。

按较少类别数量随机下采样为两类各10,997条，共21,994条，使用种子42，按标签分层随机划分90%训练集与10%验证集，分别19,794和2,200条。这是项目自行划分，未复用论文官方的域外或跨生成器测试划分；未进行提示词/来源分组划分或近重复文本去重。

| 参数 | 历史设置 |
| --- | --- |
| 最大序列长度 | 256 token，截断；训练固定长度padding |
| 学习率 | 0.00002 |
| 单设备训练/验证批量 | 8 / 16 |
| 梯度累积 | 4 |
| 权重衰减 | 0.01 |
| 训练轮数 | 3 |
| 精度 | float32，未启用fp16 |
| 选择模型 | 每轮验证，按F1选择最佳检查点 |
| 最佳检查点 | step 618，约第1轮；完成3轮后回载最佳权重 |
| 推理标签 | 0=human；1=AI-generated |
| 研究推理阈值 | 0.7 |

历史脚本的种子42用于采样和划分；模型分类头初始化前未显式设全局种子。开源版补上`set_seed`和训练参数种子，重新训练不会保证与旧权重逐位一致。

## 历史验证结果

从保存的训练指标读取：accuracy=0.984545、precision=0.971681、recall=0.998182、F1=0.984753。以上为模型选择所用的内部验证集表现，使用`argmax`规则（二分类通常等价于约0.5分界），**不是0.7阈值下的结果，也不是股吧人工标注测试集准确率**。同一验证集参与了最佳检查点选择，因此它也不是独立、未参与选择的最终测试集。

未完成目标领域人工标注测试、概率校准、短文本分层评估、时间外测试或对混合/改写文本的专项评估。不得以本项内部指标宣传“股吧识别准确率98.45%”。新增发布代码通过测试不改变这些统计限制。

发布核查按历史种子与划分规则重建样本，训练集19,789个不同文本、验证集2,200个不同文本；其中2条验证文本与训练集完全相同。该检查只比较原始清洗后文本的精确一致性，没有覆盖近重复或分词截断后的重复。这进一步限制了历史内部指标的解释，不能当作完全隔离的评测结果。

## 分数与适用边界

`AI生成概率`为softmax的AI类分数，未在股吧真实类别比例下校准；训练采用两类均衡采样，部署类别比例可能不同。分数0.7不能直接解释为真实世界有70%概率来自AI。阈值0.7是操作性设定，未记录目标领域的最优阈值搜索。

用户数据可能仅包含正文开头约203字符，之后又受256 token输入上限约束。短文本、模板化人类写作、复制转载、混合编辑及未见生成模型可能造成误判。概率求和是连续模型得分汇总，不能在缺乏校准时直接称为真实AI帖子数量的无偏估计。

## 开源推理与历史程序的差异

保留类别方向、softmax、第1类分数、默认阈值0.7、最大长度256。对非空同一文本，使用同一权重仍执行同一分类计算，存在设备/批量引起的浮点微差。

公开推理将空文本的标签和分数留空，状态为`empty_text`。历史程序试图将空字符串置0，但CSV默认缺失值可能先变成字符串`nan`，实际并非所有空单元格都被处理为0。该修正只作用于开源代码，不回写研究历史结果。

公开推理采用完整文件校验和原子写入，避免将中断文件当作已完成结果；原始训练数据、用户记录及研究面板不发布。

## 许可状态

截至2026-09-18，HFL基础模型页面标注Apache-2.0；C-ReD仓库未发现明确LICENSE，GitHub API返回`license: null`。这不等价于对衍生权重作法律上的禁止结论，但缺乏足够依据将其宣称为可任意再分发的开放权重。当前先公开原创软件，权重候选包本地保存，取得明确许可依据后再发布。

本次没有实施差分隐私训练、机器遗忘或成员推断评估。删除路径和训练附件属于发布文件的脱敏，不能证明神经网络权重从未记忆训练数据。
