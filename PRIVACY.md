# 发布文件脱敏说明

公开仓库从白名单创建，不复用研究工作区的Git历史。公开材料不包含真实帖子、用户标识、个人邮箱、IP位置、个人主页、帖子链接、股票样本清单、回归面板、商业数据库、访问凭据、原始训练日志或Notebook执行输出。

示例为全新虚构句子，仅用于软件测试。示例股票代码只演示六位编码格式，不构成真实用户数据或投资建议。维护者公开署名仅用已授权的GitHub账号`tjuwqx`，Git提交采用GitHub noreply地址。

候选模型包只允许必要的推理文件；排除`training_args.bin`、优化器状态、随机数状态、训练中间检查点、缓存、数据集与日志。模型和分词器配置中的本机路径、token等非必要字段被删除；safetensors文件头仅保留张量描述与标准`format=pt`元数据。

仓库审计程序检查白名单、文件体积、私有路径、常见令牌格式、邮箱和IPv4形式；检查结果只输出文件名与规则名，不回显疑似秘密。静态模式检查可能有漏报或误报，不能声称完成了形式化隐私证明。

原股吧数据未用于该次训练，只是历史推理输入。即便如此，对C-ReD训练语料的潜在记忆风险仍未经专门评估；本次文件脱敏不等于差分隐私或模型遗忘。

推理读取用户指定的本地CSV并本地写出结果，不调用外部模型服务。用户运行训练时，Transformers可能从上游下载基础模型；自行发布推理输出前需要另行检查原始列中的个人信息。
